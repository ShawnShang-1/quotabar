name: Release macOS App

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-release:
    name: Build QuotaBar.app
    runs-on: macos-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Show Swift toolchain
        run: swift --version

      - name: Run tests
        run: swift test

      - name: Package app
        run: Scripts/package-local-app.sh

      - name: Create zip
        run: |
          VERSION="${GITHUB_REF_NAME:-manual}"
          ditto -c -k --keepParent dist/QuotaBar.app "QuotaBar-${VERSION}-macOS.zip"

      - name: Upload release asset
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: QuotaBar-${{ github.ref_name }}-macOS.zip
          body: |
            QuotaBar macOS app build.

            This build is ad-hoc signed and not notarized. If macOS blocks it, right-click the app and choose Open, or run:

            ```bash
            xattr -dr com.apple.quarantine /Applications/QuotaBar.app
            open /Applications/QuotaBar.app
            ```

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: QuotaBar-macOS
          path: QuotaBar-*-macOS.zip
